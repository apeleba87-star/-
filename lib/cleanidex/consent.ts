/** Versioned client e-sign consent (Korean). Bump version when text changes. */
export const CLIENT_ESIGN_CONSENT_VERSION = "1";

export const CLIENT_ESIGN_CONSENT_TEXT = `본인은 본 계약서의 내용을 충분히 확인하였으며,
본 전자서명이 자필 서명과 동일한 효력을 가질 수 있음을 이해하고 이에 동의합니다.`;
